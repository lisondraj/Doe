import { StoryShaderExportView } from "@/components/story/StoryShaderExportView";

/** Dev route — renders story shaders at export resolution for Playwright capture. */
export default function StoryShaderExportPage() {
  return <StoryShaderExportView />;
}
