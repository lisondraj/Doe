import { MainpageRouteLayout } from "@/components/MainpageRouteLayout";

export default function MainpageLayout({ children }: { children: React.ReactNode }) {
  return <MainpageRouteLayout>{children}</MainpageRouteLayout>;
}
