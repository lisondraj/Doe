"use client";

import { StoryContactFooter } from "@/components/story/StoryContactFooter";

/** Bottom sidebar stack — contact card only. */
export function StorySidebarFooter() {
  return (
    <div className="story-sidebar-footer-stack mt-auto">
      <StoryContactFooter />
    </div>
  );
}
