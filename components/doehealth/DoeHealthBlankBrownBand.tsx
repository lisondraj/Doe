/** Blank viewport band — continues the brown gradient stack below the intro section. */
export function DoeHealthBlankBrownBand() {
  return (
    <section
      className="doehealth-blank-brown-band relative z-10 w-full shrink-0"
      aria-hidden
    />
  );
}
