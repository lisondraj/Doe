"use client";

import { PremedDesktopNav } from "@/components/premed/PremedDesktopNav";

/** Desktop /join — same punched gold nav chrome as /premed. */
export function CampusAmbassadorDesktopNav() {
  return <PremedDesktopNav />;
}
