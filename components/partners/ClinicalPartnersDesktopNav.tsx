"use client";

import { PremedDesktopNav } from "@/components/premed/PremedDesktopNav";

/** Desktop /partners — same punched gold nav chrome as /premed. */
export function ClinicalPartnersDesktopNav() {
  return <PremedDesktopNav />;
}
