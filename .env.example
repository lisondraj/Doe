# Primary marketing domain (default: doe.care)
PRIMARY_SITE_HOST=doe.care

# Join / waitlist domain (default: doehealth.care)
JOIN_SITE_HOST=doehealth.care

# Absolute URL for "Join Waitlist" links from the primary site
NEXT_PUBLIC_JOIN_URL=https://doehealth.care/join
