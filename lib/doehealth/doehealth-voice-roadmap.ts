/** /doehealth — Built by doctors voice-first roadmap diagram labels. */
export const DOEHEALTH_VOICE_ROADMAP = {
  focus: "Voice",
  /** Two rows of three — order left-to-right, top then bottom. */
  nextRows: [
    ["Front-desk", "Scheduling", "Prior Auth"],
    ["Referrals", "Results", "Documentation"],
  ],
} as const;
