/** Placeholder copy for product puzzle tile detail modals. */
export function storyProductPuzzlePlaceholder(product: string, slot: string): string {
  return `${product} — ${slot}. Placeholder description for this module. Final copy will explain how this part of the product supports clinic workflows, governance, and day-to-day operations.`;
}
