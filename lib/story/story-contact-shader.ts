import { DOEHEALTH_ROUTED_CALLS_LEFT_SHADER } from "@/lib/doehealth/doehealth-routed-calls-shader";
import { doeHomeHeroDuskShaderSurface } from "@/lib/proto/proto-shader-backdrop-colors";

/** /doehealth left-bleed warm pool — story James contact inset panel. */
export const STORY_CONTACT_SHADER = DOEHEALTH_ROUTED_CALLS_LEFT_SHADER;

/** /doehealth hero dusk flow — story Meet Doe modal square. */
export const STORY_MEET_DOE_MODAL_SHADER = doeHomeHeroDuskShaderSurface();
