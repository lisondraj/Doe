/** Join mobile hero — 2× closing-section card height. */
export const JOIN_MOBILE_HERO_HEIGHT =
  "min-h-[clamp(45rem,116vmin,66rem)] h-[clamp(45rem,116vmin,66rem)] iphone-page:min-h-[clamp(42rem,110vmin,62rem)] iphone-page:h-[clamp(42rem,110vmin,62rem)]";

/** Half of join hero height — intern track cards. */
export const JOIN_MOBILE_CARD_HEIGHT =
  "min-h-[clamp(22.5rem,58vmin,33rem)] h-[clamp(22.5rem,58vmin,33rem)] iphone-page:min-h-[clamp(21rem,55vmin,31rem)] iphone-page:h-[clamp(21rem,55vmin,31rem)]";
