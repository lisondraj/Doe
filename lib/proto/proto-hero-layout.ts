/** /proto hero — shorter than home so stacked feature sections start sooner. */
export const PROTO_HERO_HEIGHT =
  "calc(var(--app-vh,100lvh)*0.72 + max(5.5rem, calc(env(safe-area-inset-top, 0px) + 2.75rem)))";
