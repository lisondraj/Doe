/** Proto grain — DPR-matched PNGs at 208px (denser, SS-like). */
export const PROTO_GRAIN_TILE_PX = 208;

export const PROTO_GRAIN_BG = 'url("/proto-grain-tile.png")';
export const PROTO_GRAIN_BG_3X = 'url("/proto-grain-tile-3x.png")';

export const PROTO_GRAIN_SIZE = `${PROTO_GRAIN_TILE_PX}px ${PROTO_GRAIN_TILE_PX}px`;

/** Layer opacity — hard-light blend on gradient surfaces. */
export const PROTO_GRAIN_OPACITY = 0.46;

/** Hero-only grain — slightly lighter than feature cards. */
export const PROTO_HERO_GRAIN_OPACITY = 0.4;

export const PROTO_PHONE_GRAIN_BG = PROTO_GRAIN_BG;
